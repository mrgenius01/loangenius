// Example index.js for models to register all models
const User = require('./user');
const Loan = require('./loan');
const Transaction = require('./transaction');

module.exports = { User, Loan, Transaction };
